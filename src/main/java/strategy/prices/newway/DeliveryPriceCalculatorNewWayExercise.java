package strategy.prices.newway;

import strategy.prices.Item;
import strategy.prices.Plan;

import java.math.BigDecimal;

public class DeliveryPriceCalculatorNewWayExercise {

    public BigDecimal calculateDeliveryPrice(Item item, Plan plan) {

        return new BigDecimal(12) ; //todo implements logic
    }
}